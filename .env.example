PORT=3333
HOST=localhost
LOG_LEVEL=info
APP_KEY=your_app_key_here_min_16_chars
NODE_ENV=development

# Optional: Dev API key for bypassing rate limits during testing
# DEV_API_KEY=your_dev_key_here

# HMAC secret for verifying submissions from NOMAD installations
# Must match the secret embedded in NOMAD. Leave unset to use default.
# HMAC_SECRET=nomad-benchmark-v1-2026
